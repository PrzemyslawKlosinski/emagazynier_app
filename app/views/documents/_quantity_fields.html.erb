<li class="control-group nested-fields">
  <div class="controls">
    <%= f.label :amount, "Ilosc:" %>
    <%= f.text_field :amount %>
 
    <%= f.fields_for :product do |quantity_product| %>
      <%= quantity_product.text_field :name %> 
       <!-- < quantity_product.text_field :name, :as => :select %> -->
      <!-- < collection_select(:quantity, :product_id, @products, :id, :name, {:include_blank => false}) %> -->

      <!-- document[quantities_attributes][new_quantities][amount]& -->
    <% end %>
 
    <%= link_to_remove_association "remove", f %>
  </div>
</li>