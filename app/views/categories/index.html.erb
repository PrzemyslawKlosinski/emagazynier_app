
    <br />
      
  <div class="row-fluid">
      <div class="span6 offset3">

      <legend>Kategorie</legend>
        <div class="well sidebar-nav">
          <div class="field center">
              <%= render 'form' %> 
          </div>
        </div>
      </div>
    </div>

<!-- <div class="container-fluid"> -->
  <div class="row-fluid">
    <div class="span6 offset3">

      <table class="table table-striped">

        <tbody>
          <% @categories.each do |category| %>
          <tr>
            <td>
              <%= category.name %>
              <!-- <%= link_to category.name, :controller => "categories", :action => "index", :id => category %>  -->
              <!-- <%= category.name %>-->
            </td> 
            <td>
              <span class="pull-right">
                <% if !category.isDefault %>
                <%= link_to 'Edytuj', edit_category_path(category) %> | 
                <%= link_to 'Usuń', category, method: :delete, data: { confirm: 'Czy napewno usunąć?' } %>
                <% end %>
              </span>
            </td>
          </tr>
             <% end %>
          </tbody>

        </table>

<!-- </div> -->
</div>
</div>
