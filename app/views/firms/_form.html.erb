<%= form_for(@firm) do |f| %>

  <% if @firm.errors.any? %>
    <div id="error_explanation">
        <div class="alert alert-error">
          Znaleziono bledow <%= pluralize(@firm.errors.count, ".") %>.
        </div>
      <ul>
      <% @firm.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row">
    <div class="span5 offset1">
      Nazwa
      <%= f.text_field :name %>
    </div>
  <div class="span5">
      Adres
    <!-- < f.text_field :current_address %> -->
       <%= collection_select(:product, :current_address, @locations, :id, :address, {:prompt => 'nowy' }) %>
    </div>
  </div>

  <div class="row">
    <div class="span5 offset1">
      WWW
      <%= f.text_field :www %>
    </div>
  <div class="span5">
      Email
    <%= f.text_field :email %>
    </div>
  </div>

<div class="row">
  <span class="pull-right">
    <%= link_to 'Powrót', products_path, class:"btn btn-info" %>
    <%= f.submit "Zapisz", class:"btn btn-info" %>
  </span>
  <br />
</div>

<% end %>

